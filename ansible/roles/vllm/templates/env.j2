# Rendered by Ansible — secrets. Mode 0600. Read by `docker compose` from {{ compose_dir }}.
VLLM_API_KEY={{ vllm_api_key }}
{% if hf_token | default('') | length > 0 %}
HF_TOKEN={{ hf_token }}
{% endif %}
