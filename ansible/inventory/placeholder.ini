# Committed placeholder so ansible-lint / CI can resolve the `gpu` group.
# bin/spin writes the real host into hosts.ini (gitignored) in this same directory
# at runtime; Ansible merges all files in the inventory dir.
[gpu]
