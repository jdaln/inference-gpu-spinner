# Python tooling for the control machine (operator side).
# Install into a venv:  python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
ansible>=10,<12
ansible-lint>=24
jmespath>=1.0
